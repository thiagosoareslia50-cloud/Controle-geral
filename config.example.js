// 🛡️ Sentinel: Environment Configuration Template
// Copy this file to config.js and replace the placeholder values with your actual secrets.
// Ensure config.js is added to your .gitignore so it is not accidentally committed.

window.ENV = {
  // Supabase Configuration
  SUPABASE_URL: "https://your-project-id.supabase.co",
  SUPABASE_ANON_KEY: "your-anon-key-here"
};
